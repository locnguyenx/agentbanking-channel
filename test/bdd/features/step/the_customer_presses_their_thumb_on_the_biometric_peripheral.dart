import 'package:flutter_test/flutter_test.dart';

/// Usage: the customer presses their thumb on the Biometric Peripheral

Future<void> theCustomerPressesTheirThumbOnTheBiometricPeripheral(WidgetTester tester) async {
  // Logic: In Mock case, we might just pump or wait for auto-scrambled behavior if not triggered manually
  await tester.pumpAndSettle();
}
